import reducer from "./reducer";

import * as friendsSelectors from "./selectors";
import * as friendsThunks from "./thunks";
import * as friendsActions from "./actions";
import * as friendsActionCreators from "./actionCreators";

export {
  friendsSelectors,
  friendsThunks,
  friendsActions,
  friendsActionCreators,
};

export default reducer;
