import reducer from "./reducer";

import * as dialogsSelectors from "./selectors";
import * as dialogsThunks from "./thunks";
import * as dialogsActions from "./actions";
import * as dialogsActionCreators from "./actionCreators";

export {
  dialogsSelectors,
  dialogsThunks,
  dialogsActions,
  dialogsActionCreators,
};

export default reducer;
